import React , { Component } from 'react';
import './breadcrumbs.scss';

export default class Breadcrumbs extends Component {
    render(){
        return(
            <div {...this.props}>
                <label>Electrónica, Audio y Video > iPod > Reproductores > iPod touch > 32 GB</label>
            </div>
        );
    }
}